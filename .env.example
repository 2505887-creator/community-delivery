# Server / Prisma (do not expose DATABASE_URL to the frontend)
DATABASE_URL=

JWT_SECRET=
APP_ORIGIN=http://localhost:3000

# Supabase (server)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Supabase (browser client only — public anon key)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
